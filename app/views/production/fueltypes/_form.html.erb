<%= form_for([:production,@fueltype], html: {class: 'form-horizontal'}) do |f| %>
  <% if @fueltype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fueltype.errors.count, "error") %> prohibited this fueltype from being saved:</h2>

      <ul>
      <% @fueltype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class='control-group'>
    <div class='control-label'>
      <%= f.label :tipo %>
    </div>
    <div class='controls'>
      <%= f.text_field :tipo %>
    </div>
  </div>
  <div class='control-group'>
    <div class='control-label'>
      <%= f.label :combustible %>
    </div>
    <div class='controls'>
      <%= f.text_field :combustible %>
    </div>
  </div>
  <div class='control-group'>
    <div class='control-label'>
      <%= f.label :precio %>
    </div>
    <div class='controls'>
      <%= f.text_field :precio, class: 'input-small' %>
    </div>
  </div>

  <div class='control-group'>    
    <div class='controls'>
      <%= f.submit class: 'btn btn-success' %>
    </div>
  </div>

<% end %>
