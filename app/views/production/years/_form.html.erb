<%= form_for([:production,@year], html: { class: 'form-horizontal'}) do |f| %>
  <% if @year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@year.errors.count, "error") %> prohibited this anho from being saved:</h2>

      <ul>
      <% @year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='control-group'>
    <div class='control-label'>
      <%= f.label :numero %>
    </div>
    <div class='controls'>
      <%= f.text_field :nombre %>
    </div>
  </div>

  <div class='control-group'>    
    <div class='controls'>
      <%= f.submit class: 'btn' %>  
    </div>
  </div>
  
<% end %>
