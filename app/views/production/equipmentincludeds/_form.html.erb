<%= form_for([:production,@equipmentincluded], :html => {:class =>'form-horizontal'}) do |f| %>
  <% if @equipmentincluded.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipmentincluded.errors.count, "error") %> prohibited this equipmentincluded from being saved:</h2>

      <ul>
      <% @equipmentincluded.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <div class="control-label">
      <%= f.label :nombre %>
    </div>
    <div class="controls">
      <%= f.text_field :nombre %>
    </div>
  </div>
  <div class="control-group">
    <div class="controls">
      <%= f.submit :class => 'btn btn-success' %>
    </div>
  </div>
<% end %>
